export enum SettingsTheme {
	DARK = "DARK",
	LIGHT = "LIGHT",
	SYSTEM = "SYSTEM",
}

export enum OrderByDirection {
	ASC = "ASC",
	DESC = "DESC",
}

export enum SettingsListStyle {
	GRID = "GRID",
	LIST = "LIST",
}

export enum SongsOrderByField {
	TITLE = "TITLE",
	ALBUM = "ALBUM",
	RELEASED = "RELEASED",
	DURATION = "DURATION",
}
export enum GenresOrderByField {
	NAME = "NAME",
}

export enum AlbumsOrderByField {
	TITLE = "TITLE",
	RELEASED = "RELEASED",
}

export enum ArtistsOrderByField {
	NAME = "NAME",
}

export enum PlaylistsOrderByField {
	TITLE = "TITLE",
}

export enum LibrarySongsOrderByField {
	TITLE = "TITLE",
	ALBUM = "ALBUM",
	DURATION = "DURATION",
	RELEASED = "RELEASED",
	DATE_ADDED = "DATE_ADDED",
}

export enum LibraryArtistsOrderByField {
	NAME = "NAME",
	DATE_ADDED = "DATE_ADDED",
}

export enum LibraryPlaylistsOrderByField {
	TITLE = "TITLE",
	DATE_ADDED = "DATE_ADDED",
}