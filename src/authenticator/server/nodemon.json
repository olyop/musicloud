{
	"$schema": "https://json.schemastore.org/nodemon.json",
	"quiet": true,
	"ext": "ts,sql,env",
	"ignore": [
		"webpack",
		"scripts",
		".vscode",
		"src/types",
		"src/player",
		"src/uploader",
		"src/accounts",
		"src/authenticator/client"
	]
}
